@model PLTour.Shared.Models.Entities.Product
@{
    ViewData["Title"] = "Thêm sản phẩm";
    Layout = "_VendorLayout";
}

<div class="container-fluid">
    <div class="row justify-content-center">
        <div class="col-md-8">
            <div class="card">
                <div class="card-header bg-primary text-white">
                    <h4 class="mb-0"><i class="fas fa-plus-circle me-2"></i>@ViewData["Title"]</h4>
                </div>
                <div class="card-body">
                    <form asp-action="Create" method="post" enctype="multipart/form-data">
                        @Html.AntiForgeryToken()
                        <div asp-validation-summary="ModelOnly" class="alert alert-danger"></div>

                        <div class="mb-3">
                            <label asp-for="Name" class="form-label fw-bold">Tên sản phẩm <span class="text-danger">*</span></label>
                            <input asp-for="Name" class="form-control" placeholder="Ví dụ: Phở bò tái, combo, menu item..." />
                            <span asp-validation-for="Name" class="text-danger"></span>
                        </div>

                        <div class="mb-3">
                            <label asp-for="Description" class="form-label fw-bold">Mô tả</label>
                            <textarea asp-for="Description" class="form-control" rows="4" placeholder="Mô tả sản phẩm, món, dịch vụ..."></textarea>
                            <span asp-validation-for="Description" class="text-danger"></span>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label asp-for="Price" class="form-label fw-bold">Giá <span class="text-danger">*</span></label>
                                    <div class="input-group">
                                        <input asp-for="Price" class="form-control" type="number" step="1000" placeholder="0" />
                                        <span class="input-group-text">đ</span>
                                    </div>
                                    <span asp-validation-for="Price" class="text-danger"></span>
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label asp-for="StoreId" class="form-label fw-bold">Cửa hàng <span class="text-danger">*</span></label>
                                    <select asp-for="StoreId" class="form-select" asp-items="@(new SelectList(ViewBag.Stores, "StoreId", "StoreName"))">
                                        <option value="">-- Chọn cửa hàng --</option>
                                    </select>
                                    <span asp-validation-for="StoreId" class="text-danger"></span>
                                </div>
                            </div>
                        </div>

                        <div class="row">
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label asp-for="StockQuantity" class="form-label">Số lượng tồn</label>
                                    <input asp-for="StockQuantity" class="form-control" type="number" min="0" />
                                </div>
                            </div>
                            <div class="col-md-6">
                                <div class="mb-3">
                                    <label class="form-label fw-bold">Ghi chú</label>
                                    <input class="form-control" value="Sản phẩm sẽ tự thuộc về cửa hàng đã chọn" disabled />
                                </div>
                            </div>
                        </div>

                        <div class="mb-3">
                            <label class="form-label fw-bold">Hình ảnh món ăn</label>
                            <input type="file" name="imageFile" class="form-control" accept="image/*" />
                            <small class="text-muted">Chấp nhận .jpg, .png (không bắt buộc)</small>
                        </div>

                        <div class="form-check mb-3">
                            <input asp-for="IsAvailable" class="form-check-input" checked />
                            <label asp-for="IsAvailable" class="form-check-label">Hiển thị (còn hàng)</label>
                        </div>

                        <hr />
                        <div class="d-flex justify-content-between">
                            <a href="/vendor/products" class="btn btn-secondary">
                                <i class="fas fa-arrow-left me-1"></i>Quay lại
                            </a>
                            <button type="submit" class="btn btn-primary">
                                <i class="fas fa-save me-1"></i>Thêm món ăn
                            </button>
                        </div>
                    </form>
                </div>
            </div>
        </div>
    </div>
</div>